spring:
  application:
    name: stock-service

  datasource:
    url: jdbc:mysql://localhost:3306/stock_service_db
    username: root
    password: mysql123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

services:
  company:
    url: http://localhost:8080
  warehouse:
    url: http://localhost:8081
  product:
    url: http://localhost:8082
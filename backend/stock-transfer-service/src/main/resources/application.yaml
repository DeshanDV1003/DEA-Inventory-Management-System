server:
  port: 8080

spring:
  application:
    name: stock-transfer
  datasource:
    url: jdbc:mysql://localhost:3306/stock_transfer_db?createDatabaseIfNotExist=true
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true